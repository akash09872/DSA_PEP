// Infix to postfix

// 1. operand -> Append in string ans;
// 2 -> ( push in stack
// 3. ) -> pop from and add to ans until (
// 4 -> Operator -> pop while the top of stack has greater or equal or precedence, the push.